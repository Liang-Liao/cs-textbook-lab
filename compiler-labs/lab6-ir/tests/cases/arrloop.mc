// 循环体内声明的数组每轮必须重新清零——"声明即零初始化"在循环
// 重进入下也成立。曾因 gen 对数组不发指令、irvm 按名惰性分配一次，
// 把上一轮的 a[0]=9 带进下一轮（irvm 输出 0,9，eval 输出 0,0）。
int i = 0;
while (i < 2) {
    int a[2];
    print a[0];
    a[0] = 9;
    i = i + 1;
}
print i;
