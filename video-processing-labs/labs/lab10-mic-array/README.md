# Lab 10 — 麦克风阵列（选做）

GCC-PHAT 测向 + 4 麦 / 8 阵元延迟求和波束形成。

## 过关判据

| 指标 | 判据 |
|------|------|
| 定位（GCC-PHAT） | ±60° 内误差 ≤ 5° |
| 4 麦 DAS 波束 | 目标/抑制方向增益差 ≥ 6 dB |

```bash
make && make test
```

输出 `out/beam_pattern.pgm`：上半为 4 麦方向图，下半为 8 阵元理论方向图。
