// LICM × 展开 组合回归：g*n 是循环不变量（先被外提到 preheader，
// 体内只剩对 t1 的"使用"），随后的展开克隆体必须继续共享 t1 原名——
// 若把该使用也改名，克隆体将引用一个从未定义的临时（读到垃圾值）。
// 语义答案：8 轮 × 35 = 280。
int g = 7;
int n = 5;
int acc = 0;
for (int i = 0; i < 8; i = i + 1)
    acc = acc + g * n;
print acc;
