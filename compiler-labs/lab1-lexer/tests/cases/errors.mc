// 词法错误演示：一次报出所有错误（词法级错误恢复 = 报错并跳过）
int a = 1;
int b@ = 2;        // 第 3 行：非法字符 '@'
float c = 1.2.3;   // 第 4 行：病态数字
float d = 456abc;  // 第 5 行：数字后直接跟字母
int e = a & b;     // 第 6 行：单字符 '&'
float f = 3.14;    // 正常行
/* 这个块注释没有闭合
