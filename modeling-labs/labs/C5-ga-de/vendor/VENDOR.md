# vendor（前序 lab 业务算法 copy）

本 lab **C5-ga-de** 编译使用本目录；正本在下列前序 lab 的 `src/`。
只读：不要在 vendor 写业务代码。更新依赖时重跑 sync_vendor。

- 来自 `A2-prob-stat/src`: dist.h, dist.c, stats.h, stats.c, gof.h, gof.c
- 来自 `C1-rng-mc/src`: rng.h, rng.c, mc.h, mc.c
- 来自 `C3-sa/src`: bench_sa.h, bench_sa.c
- 来自 `B2-newton-quasi-tr/src`: bench.h, bench.c
